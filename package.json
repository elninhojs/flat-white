{
  "name": "flat-white",
  "version": "0.1.1",
  "main": "src/index.mjs",
  "scripts": {
    "start": "FW_PORT=8111 FW_DIR=./mock-files/ node src/index.mjs",
    "test": "npm start & jest",
    "build": "echo 'Not yet!'",
    "release": "node ./scripts/release.js",
    "stop": "pkill -f \"flat-white-api\" && exit 0"
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "jest": "^28.1.3",
    "node-rest-client": "^3.1.1"
  }
}
